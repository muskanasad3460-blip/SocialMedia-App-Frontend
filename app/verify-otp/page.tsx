import OTPForm from "@/components/auth/OTPForm";

export default function VerifyOTPPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gradient-to-r from-gray-200 to-indigo-200">
      <OTPForm />
    </main>
  );
}
